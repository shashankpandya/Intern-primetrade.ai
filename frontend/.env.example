# For local development, the frontend will automatically use http://localhost:5000/api/v1
# For production deployment, set the backend URL here (or leave empty to use relative path /api/v1)
# This assumes both frontend and backend are deployed to the same domain or using a reverse proxy.
# VITE_API_BASE_URL="https://your-backend-api.example.com/api/v1"
